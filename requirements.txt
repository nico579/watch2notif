# Versions figees sur les paquets verifies (testes ici, sur Windows) :
# build.py cree un venv neuf a chaque construction, sans pin la version
# resolue peut driver d'une reconstruction a l'autre (mois d'ecart, un
# nouveau majeur sort) et rendre le bundle plus "reproductible d'une
# machine a l'autre" que "reproductible d'une construction a l'autre".
# pync/plyer restent sans version : jamais installes/testes sur cette
# machine (Windows), les figer a l'aveugle risquerait de coincer sur une
# version cassee plutot que d'aider.
feedparser==6.0.14
platformdirs==4.11.9
win11toast==0.36.3; sys_platform == "win32"
pync; sys_platform == "darwin"
plyer; sys_platform == "linux"
# Meme version que lidar2map/blink2video (verifiee sur cette machine) :
# icone de zone de notification, remplace PySide6/QSystemTrayIcon.
pystray==0.19.5
# Utilise a l'execution par pystray (charge assets/watch2notif.png comme
# image d'icone) ET par PyInstaller au moment de la construction pour
# produire l'icone .exe/.icns native (watch2notif.spec) : plus seulement
# un outil de build depuis le retrait de Qt.
Pillow==12.3.0
